/*
 * Programming Quiz: Laugh it Off 2 (5-2)
 */
function laugh(num) {
    var laughter = "";
    for (var x = 0; x < num; x++) {
        laughter += "ha";

    }
    return laughter;
}
return (laugh(3) + "!");
// declare your function
// then, call it!
// print the output with console.log